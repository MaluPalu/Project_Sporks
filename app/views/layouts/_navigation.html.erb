<div class="navbar-fixed">

  <nav>
    <div class="nav-wrapper row">
      <div class="col s4">
        <a href="<%= root_path%>">  <img class="brand-logo" src="<%= image_path"food_header/TheSpork_Logo.png"%>" style="height: 100%; padding-left: 20px"> </a>
      </div>

      <div class="col s4">
          <form action="/recipes" method="get" class="">
            <div class="row" id="topbarsearch">
              <div class="input-field  blue-text">
                <i id="si" class="orange-text material-icons prefix ">search</i>
                <input class="col" id="recipes_search" autocomplete="off" type="text" name="term" placeholder="search"/>
              </div>
            </div>
          </form>
      </div>

      <div >
      <ul id="nav-mobile" class="right hide-on-med-and-down">
      <% if current_user %>
        <li><%= link_to "#{'Hello, ' + current_user.username}", user_path(current_user) %></li>
         <li><%= link_to'Recipes', recipes_path%></li>
        <li><%= link_to'Log Out', destroy_user_session_path, :method=> 'delete' %></li>
      <% else %>
        <li><%= link_to 'Sign In', new_user_session_path %></li>
      <li><%= link_to'Sign Up', new_user_registration_path%></li>
      <% end %>
      </ul>
    </div>



    </div>
  </nav>
</div>
